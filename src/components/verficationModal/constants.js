const USER_TYPE = {
    CIVILIAN: "For civilian the ticket price is Rs.10000",
    REGISTERED_STUDENT: "For IITG registred students the ticket price is Rs. 500"
}

const EMAIL_REGEX = /^([a-zA-Z0-9_\-\.]+)@([a-zA-Z0-9_\-\.]+)\.([a-zA-Z]{2,4})$/;;

export {USER_TYPE, EMAIL_REGEX};